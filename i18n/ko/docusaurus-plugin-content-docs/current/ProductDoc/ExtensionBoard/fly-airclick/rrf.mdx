---
sidebar_position: 5
sidebar_label: RRF에 연결하기
---

# RRF에 연결하기

    :::info 중요한 팁
    * AirClick는 대상 프린터와 동일한 네트워크에 연결되어야 합니다.
    :::

1. 왼쪽 시스템 설정을 클릭합니다.

<ImageView image={require('@site/docs/ProductDoc/ExtensionBoard/fly-airclick/img/setting.webp').default} size="80%" align="Center" />

2. `Printer Type`을 `RRF`로 선택합니다.
3. `RepRapFirmware IP`에 RRF 기계의 IP 주소를 입력합니다.

    <ImageView image={require('@site/docs/ProductDoc/ExtensionBoard/fly-airclick/img/rrf.webp').default} size="80%" align="Center" />

4. 위와 같이 구성한 후 오른쪽 아래의 `제출`을 클릭하여 구성을 저장합니다.
5. 정확하게 연결되면 웹 페이지 상단에 알림이 표시됩니다.

     <ImageView image={require('@site/docs/ProductDoc/ExtensionBoard/fly-airclick/img/setting-3.webp').default} size="80%" align="Center" />

6. 연결에 실패한 경우
    - IP 주소가 올바른지 확인합니다.
    - 프린터와 AirClick가 동일한 네트워크에 연결되어 있는지 확인합니다.
    - RRF 메인보드가 정상적으로 작동하는지 확인합니다.
    - 브라우저에서 RRF의 웹페이지를 열 수 있는지 확인합니다. 
---
sidebar_position: 6
sidebar_label: 烧录Katapult固件
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import ImageView from '@site/src/components/ImageView';
import WIN from '@site/docs/General/dfu-flash/_pc-uf2-flash.mdx';
import Katapult from '@site/docs/General/dfu-flash/_host-uf2-flash.mdx';
import CANBOOT from '@site/docs/General/dfu-flash/_firmware/katapult/_katapult.mdx';

# Katapult固件

## Katapult固件说明

    <CANBOOT />

    :::tip

    * SB2040_PRO_V3 支持CAN与RS232
    * 这边只提供CAN固件分500K与1M速率
    :::

## 进入烧录模式

    :::tip

    左边为`RESET按键`，右边为`BOOT按键`

    :::

    * 进入烧录模式有两种方法
    * 第一种：主板完全断电情况下，按着BOOT按键然后将带有数据传输的数据线一头接主板一头接你设备
    * 第二种：在将带有数据传输的数据线一头接主板一头接你设备后，按着BOOT键然后按下RST键，然后松开RST键最后松开BOOT键
    <ImageView image={require('./img/boot.png').default} size="100%" align="center" />

## 烧录固件
    <Tabs>
        <TabItem value="win" label="windows烧录教程" default>

            * SB2040 PRO V3的Katapult固件下载链接
            ```bash
            https://cdn.mellow.klipper.cn/firmware/BL/SB2040_PRO_V3.zip
            ```
            <WIN />
            
        </TabItem>
        <TabItem value="linux" label="上位机烧录教程">
            
            <Katapult />

            4. 烧录固件

            :::tip

            只需要烧录其中一个固件

            :::

            * 下方是500K CAN速率的固件刷写指令
            ```bash
            sudo ~/klipper/lib/rp2040_flash/rp2040_flash ~/FLY_Katapult/BL/SB2040/FLY_SB2040PROV3_CANBOOT_CAN_500K.uf2
            ```
                       
            * 下方是1M CAN速率的固件刷写指令
            ```bash
            sudo ~/klipper/lib/rp2040_flash/rp2040_flash ~/FLY_Katapult/BL/SB2040/FLY_SB2040PROV3_CANBOOT_CAN_1M.uf2
            ```


            5. 出现下方提示则烧录成功

            <ImageView image={require('@site/docs/General/dfu-flash/img/flash3.png').default} size="100%" align="center" />

            :::tip

            烧录完成后，需要给主板彻底断电一次。

            :::

        </TabItem>
    </Tabs>

## 确认固件是否启动

    * 如果固件正常启动LED会闪烁起来
    <ImageView image={require('./img/led.png').default} size="100%" align="center" />


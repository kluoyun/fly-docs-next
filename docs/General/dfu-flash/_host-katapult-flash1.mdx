
<Tabs>
    <TabItem value="w" label="USB固件的烧录" default>
        * 使用下方指令搜索你的主板ID，应出现下图所示ID（注：图中所示ID仅供参考，每张主板的ID都不相同）

        ```bash
        ls /dev/serial/by-id/*
        ```

        :::tip 注意
            下图中参考id`/dev/serial/by-id/usb-Klipper_rp2040_12345-if00`为主板的ID
        :::

        <ImageView image={require('@site/docs/General/dfu-flash/img/ck.webp').default} size="80%" align="center" />

        * 更新USB固件需要在编译好最新固件后，输入下方命令更新，*请将`<你的主板ID>`替换为上一步查询到的ID*

        ```bash
        cd ~/klipper/ && make flash FLASH_DEVICE=<你的主板ID>
        ```

        <ImageView image={require('@site/docs/General/dfu-flash/img/katapult5.webp').default} size="100%" align="center" />
    </TabItem>
</Tabs> 

## 刷错固件解决方法
    * 按住按钮进入dfu，电脑会重新一个u盘，固件拖进去即可
    * 使用WINDOWS清理固件后重新刷固件

---
sidebar_position: 5
sidebar_label: ID-Suchanleitung
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import ImageView from '@site/src/components/ImageView';

import GetUsbID from '@site/i18n/de/docusaurus-plugin-content-docs/current/General/get-id/_get-usb-id.mdx';
import GetRS232ID from '@site/i18n/de/docusaurus-plugin-content-docs/current/General/get-id/_get-rs232-id.mdx';
import GetCanbusUUID from '@site/i18n/de/docusaurus-plugin-content-docs/current/General/get-id/_get-canbus-uuid.mdx';




# Lesen und Anzeigen der Hauptplatinen-ID

    :::tip

    Bitte ändern Sie die Konfiguration nicht, während die Webseite übersetzt wird.

    :::

    :::warning Wichtiger Hinweis

    Bitte ändern Sie die Konfiguration nicht, während die Webseite übersetzt wird.

    :::

    :::danger Zu beachten

    Bitte ändern Sie die Konfiguration nicht, während die Webseite übersetzt wird.

    :::

<Tabs>
    <TabItem value="can" label="CANBus UUID abrufen">

    * Verwenden Sie vor der ID-Suche das XT30-Datenkabel, um die Werkzeugplatine mit UTOC oder einer Hauptplatine mit Bridge-Firmware zu verbinden. Unten ist das Referenzanschlussdiagramm
        <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-sb2040/sb2040/flash/img/sb2040_utoc.png').default} size="80%" align="left" />
    * Stellen Sie sicher, dass der Widerstandswert von CAN H und CAN L bei ausgeschaltetem Strom mit einem Multimeter gemessen etwa `60Ω` beträgt. Wenn er `120Ω` beträgt, müssen Sie den `120Ω`-Jumper anschließen. Wenn er etwa `40Ω` beträgt, muss der Jumper entfernt werden.
        <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-sb2040/sb2040/flash/img/120.png').default} size="50%" align="center" />
    
        <GetCanbusUUID />

    </TabItem>
</Tabs>


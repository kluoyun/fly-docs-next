## Search for USB ID
1. Open the browser and enter the IP address of the host computer in the address bar, for example, if the IP address of my host computer is `192.168.101.179`, simply enter it and press Enter.
  <ImageView image={require('@site/docs/General/get-id/img/wed/ip.webp').default} size="80%" align="center" /> 

2. Open the WEB interface of the host computer, find in the configuration options on the left sidebar:
  <table>
    <tr>
      <td>
        - For fluidd: `{…}`, click to enter, you can find `printer.cfg`
        <ImageView image={require('@site/docs/General/get-id/img/fluidd-cfg.webp').default} size="100%" align="center" />
      </td>
      <td>
        - For mainsail: `Machine`, click to enter, you can find `printer.cfg`
        <ImageView image={require('@site/docs/General/get-id/img/mainsail-cfg.webp').default} size="100%" align="center" />
      </td>
    </tr>
  </table>

  3. Click into `printer.cfg`, then click `DEVICES` on the top right
    <table>
      <tr>
        <td>
          <ImageView image={require('@site/docs/General/get-id/img/wed/fluidd_devices.webp').default} size="100%" align="center" />
        </td>
        <td>
          <ImageView image={require('@site/docs/General/get-id/img/wed/mainsail_devices.webp').default} size="100%" align="center" />
        </td>
      </tr>
    </table>

  4. Click `SERIAL`, then refresh
    <table>
      <tr>
        <td>
          - Refresh for fluidd
          <ImageView image={require('@site/docs/General/get-id/img/wed/fluidd_refresh.webp').default} size="100%" align="center" />
        </td>
        <td>
          - Refresh for mainsail
          <ImageView image={require('@site/docs/General/get-id/img/wed/mainsail_refresh.webp').default} size="100%" align="center" />
        </td>
      </tr>
    </table>

  5. Copy the ID, click the arrow to copy
    <table>
      <tr>
        <td>
          - For fluidd, check whether `ttyAMC` exists and ensure Path by ID exists, this is the USBID
          <ImageView image={require('@site/docs/General/get-id/img/wed/fluidd_id.webp').default} size="100%" align="center" />
        </td>
        <td>
          - For mainsail, check whether `ttyAMC` exists and ensure Path by ID exists, this is the USBID
          <ImageView image={require('@site/docs/General/get-id/img/wed/mainsail_id.webp').default} size="100%" align="center" />
        </td>
      </tr>
    </table>

## Fill in USB ID

    1. Close `DEVICES` and fill in the ID

    :::danger Note
      * Please modify the configuration file as follows:  
      ```
      [mcu] 
      serial: /tmp/klipper_host_mcu
      ```  
      to
      ```
      [mcu host] 
      serial: /tmp/klipper_host_mcu
      ```
      * Add:
      ```
      [mcu]
      serial: <replace with the ID you just queried>
      ```
      :::
    <table>
      <tr>
        <td>
          - Find [MCU] in fluidd
          <ImageView image={require('@site/docs/General/get-id/img/wed/fluidd_cfg.webp').default} size="100%" align="center" />
        </td>
        <td>
          - Find [MCU] in mainsail
          <ImageView image={require('@site/docs/General/get-id/img/wed/mainsail_cfg.webp').default} size="100%" align="center" />
        </td>
      </tr>
    </table>


    2. Fill in the ID in the configuration
    :::tip

    * After filling in the ID, click `SAVE & RESTART` on the top right
    :::
    <table>
      <tr>
        <td>
          <ImageView image={require('@site/docs/General/get-id/img/wed/fluidd_save.webp').default} size="100%" align="center" />
        </td>
        <td>
          <ImageView image={require('@site/docs/General/get-id/img/wed/mainsail_save.webp').default} size="100%" align="center" />
        </td>
      </tr>
    </table>
    3. If Klipper prompts `ADC out of range`, this is a normal phenomenon. Connect the heated bed and thermistor to the motherboard, configure the thermistor pins for the nozzle and heated bed, then save and restart.

:::danger
Note: All IDs mentioned in the document are examples. Each motherboard has a different ID, please fill in the actual obtained ID.
:::
---

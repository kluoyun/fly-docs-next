Hier sind die Schritte zur Kompilierung der Firmware:

*   Verbinden Sie sich über SSH und geben Sie die folgenden Befehle ein:

    ```bash
    cd ~/klipper && rm -rf ~/klipper/.config && rm -rf ~/klipper/out && make menuconfig
    ```

*   Dabei löscht `rm -rf ~/klipper/.config && rm -rf ~/klipper/out` die vorherigen Kompilierungsdaten und Firmware. `make menuconfig` startet die Firmware-Kompilierung. Nach der Ausführung sollte das folgende Interface erscheinen:

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/img/make1.png').default} size="100%" align="Center" />

*   Wählen Sie **Enable extra low-level configuration options** und drücken Sie Enter:

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/img/make2.png').default} size="100%" align="Center" />

*   Gehen Sie zum Menü **Micro-controller Architecture** und wählen Sie **STMicroelectronics STM32**:

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/img/make3.png').default} size="100%" align="Center" />

*   Gehen Sie zum Menü **Processor model** und wählen Sie **STM32F407**:

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/407/img/407.png').default} size="100%" align="Center" />

*   Wählen Sie **Bootloader offset** und **32KiB bootloader**:

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/407/img/32kb.png').default} size="100%" align="Center" />

*   **Communication interface**, wählen Sie **USB (on PA11/PA12)**:

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/407/img/usb.png').default} size="100%" align="Center" />
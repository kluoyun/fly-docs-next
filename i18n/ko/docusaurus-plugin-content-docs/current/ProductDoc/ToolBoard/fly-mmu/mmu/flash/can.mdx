---
sidebar_position: 3
sidebar_label: CAN 펌웨어 플래싱
---

import CANFLASH from '@site/i18n/ko/docusaurus-plugin-content-docs/current/General/dfu-flash/_host-can-flash.mdx';
import MakeOperate from "/docs/General/klipper-make/_make-operate.mdx";
import CAN from '@site/i18n/ko/docusaurus-plugin-content-docs/current/General/dfu-flash/_firmware/723/_can.mdx';
import SSH from '@site/i18n/ko/docusaurus-plugin-content-docs/current/General/dfu-flash/_firmware/_ssh.mdx';

import LS from '@site/i18n/ko/docusaurus-plugin-content-docs/current/General/dfu-flash/_can.mdx';

# CAN 펌웨어 컴파일

## 컴파일 시작

    <SSH />

    <MakeOperate/>

    <CAN />

    * 마이크로 컨트롤러 시작 시 설정할 GPIO 핀 선택, 입력: !PA15 (주의: 대문자임에 유의하세요)
        <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-mmu/mmu/flash/img/can.webp').default} size="100%" align="center" />
    * 이제 설정이 저장되었으며 명령줄 화면으로 돌아왔습니다.
    * 아래 명령어를 입력하여 컴파일을 시작합니다. 시간이 다소 소요됩니다.
    ```bash
    make -j4
    ```
        <ImageView image={require('@site/docs/General/dfu-flash/img/make.webp').default} size="50%" align="center" />
    * 최종적으로 다음 내용이 출력되면 컴파일 성공입니다.

        ```bash
        Linking out/klipper.elf
        Creating hex file out/klipper.bin
        ```

## 장치 검색

    <LS />


## 펌웨어 플래싱

    * 펌웨어 플래싱 전에 XT30 데이터 케이블을 도구 보드를 UTOC 또는 브리지 펌웨어가 설치된 메인보드에 연결합니다. 참고 접선도는 아래에 있습니다.
    * 다이얼 스위치가 올바른 위치에 있는지 확인하십시오.
        <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-mmu/mmu/flash/img/can_mode.webp').default} size="90%" align="center" />
    * UTOC에 연결하는 방법은 다음과 같습니다.
        <ImageView image={require('@site/docs/General/get-id/img/utoc.webp').default} size="80%" align="left" />
    * CAN H와 CAN L의 저항값이 전원이 꺼진 상태에서 약 `60Ω`인지 멀티미터로 측정해야 합니다. 만약 `120Ω`라면 `120Ω` 점퍼를 연결해야 하며, 약 `40Ω`라면 점퍼를 제거해야 합니다.
        <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-mmu/mmu/flash/img/120.webp').default} size="50%" align="center" />

    <CANFLASH />

## 펌웨어 정상 실행 여부 확인

    * 펌웨어가 정상적으로 시작되면 LED가 켜집니다.
        <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-mmu/mmu/flash/img/led.webp').default} size="100%" align="center" />


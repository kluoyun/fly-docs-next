---
sidebar_position: 6
sidebar_label: RRF構成設定
---

# RRF構成設定

## フィラーデンタルと詰まり検出機能の設定

<ImageView image={require('@site/docs/ProductDoc/ExtensionBoard/fly-mdm/img/rrf.webp').default} size="70%" align="center" />

1. RRFプリンターのウェブインターフェースを開き、`Files` → `System` に進み、`config.g` ファイルを見つけ、クリックしてファイルを開いて編集します。
2. ファイルに以下のコードを追加してフィラーデンタル検出機能を有効にします。

```gcode
M591 D0 P2 C"PA9" S1
```

3. ファイルに以下のコードを追加して詰まり検出機能を有効にします。（`L` パラメーターは使用するエンコーダーによって異なります。誤検出が発生した場合は適切にこの値を増やしてください）

```gcode
M591 D0 P7 C"PA10" L3.5 R75:125 S1
```




    * 短接或者长按BOOT，使用type c连接到上位机后输入`lsusb`会出现`2e8a:0003` 
    * 请注意当前RP2040是处于固件烧录状态，只能烧后缀名为uf2的固件，当固件烧录进去后会消失
    * 弹出`RPI-RP2`后不需要在短接BOOT或者一直按BOOT按钮
        <ImageView image={require('@site/docs/General/get-id/img/2e8a.png').default} size="80%" align="Center" /> 
    * 通过下方命令将固件烧录到RP2040

        ```bash
        cd && cd ~/klipper/
        make flash FLASH_DEVICE=2e8a:0003
        ```
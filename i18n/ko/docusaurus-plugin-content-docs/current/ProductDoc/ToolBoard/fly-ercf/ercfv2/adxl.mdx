---
sidebar_position: 6
sidebar_label: 가속도계 사용
---

# 가속도계 사용


import ADXL from "/docs/DebugDoc/faq/kipper-adxl.mdx";

## Klipper 전환

    * 아직 병합되지 않았기 때문에 Klipper를 전환해야 사용할 수 있습니다.

    ```bash
    sudo cp -r klipper klipperdev
    sudo rm -rf ~/klipper
    git clone https://github.com/FLYmaker/klipper.git
    sudo systemctl restart klipper
    ```

    * 참고 설정
    ```cfg
    #####################################################################
    #                       LIS3DH 가속도계 구성 (활성화 필요시)
    #####################################################################
    [lis2dw]
    is_lis3dh:True
    cs_pin: SHT36:gpio9
    spi_software_sclk_pin: SHT36:gpio10
    spi_software_mosi_pin: SHT36:gpio11
    spi_software_miso_pin: SHT36:gpio12

    [resonance_tester]
    accel_chip: lis2dw
    probe_points:
        125, 125, 20  # 이 위치는 공명 측정 시 프린터 헤드가 있는 좌표입니다.
    ```

## 사용 설명서



<ADXL />
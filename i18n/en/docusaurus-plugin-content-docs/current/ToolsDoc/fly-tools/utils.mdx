---
sidebar_position: 5
sidebar_label: Generating Load Charts, etc.
---

import ImageView from '@site/src/components/ImageView';


# Function Demonstration

    <ImageView image={require('@site/docs/ToolsDoc/fly-tools/img/utils.gif').default} size="100%" align="left" />

    * Supports generating Klipper default load charts
    * Supports generating MCU frequency charts
    * Supports generating system load charts
    * Supports generating temperature curve charts
    * Supports generating frequency response charts, can also generate charts directly from existing files


# If Missing `matplotlib` is Prompted, Execute the Following Command

    ```bash
    ~/klippy-env/bin/pip install -v matplotlib
    ```
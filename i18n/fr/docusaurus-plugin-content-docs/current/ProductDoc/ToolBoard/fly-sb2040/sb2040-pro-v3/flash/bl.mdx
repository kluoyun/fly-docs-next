---
position-sidebar: 6
label-sidebar: BL firmware, ignore!
---

import WIN from '@site/i18n/fr/docusaurus-plugin-content-docs/current/General/dfu-flash/_pc-uf2-flash.mdx';
import Katapult from '@site/i18n/fr/docusaurus-plugin-content-docs/current/General/dfu-flash/_host-uf2-flash.mdx';
import CANBOOT from '@site/i18n/fr/docusaurus-plugin-content-docs/current/General/dfu-flash/_firmware/katapult/_katapult.mdx';

# Firmware Katapult

## Explication du firmware Katapult

    <CANBOOT />

    :::tip

    * SB2040_PRO_V3 prend en charge CAN et RS232
    * Seule la version CAN est fournie ici, à des vitesses de 500K et 1M
    :::

## Entrer en mode de programmation

    :::tip

    À gauche se trouve le bouton `RESET`, à droite se trouve le bouton `BOOT`

    :::

    * Il existe deux méthodes pour entrer en mode de programmation
    * Première méthode : Éteignez complètement la carte mère, appuyez sur le bouton BOOT puis branchez un câble de transmission de données entre la carte mère et votre appareil
    * Deuxième méthode : Branchez un câble de transmission de données entre la carte mère et votre appareil, puis appuyez sur le bouton BOOT et appuyez sur le bouton RST, relâchez ensuite le bouton RST puis le bouton BOOT
    <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-sb2040/sb2040-pro-v3/flash/img/boot.webp').default} size="100%" align="center" />

## Flasher le firmware
    <Tabs>
        <TabItem value="win" label="Tutoriel de flashage pour Windows" default>

            * Le firmware Katapult pour SB2040 PRO V3 <Button variant="contained" disableElevation href="https://cdn.mellow.klipper.cn/firmware/BL/SB2040_PRO_V3.zip">Cliquez pour télécharger automatiquement le firmware</Button>
            <WIN />
            
        </TabItem>
        <TabItem value="linux" label="Tutoriel de flashage pour l'interface supérieure">
            
            <Katapult />

            4. Flasher le firmware

            :::tip

            Il n'est nécessaire de flasher qu'un seul firmware

            :::

            * Ci-dessous est la commande de flashage du firmware pour une vitesse CAN de 500K
                ```bash
                sudo ~/klipper/lib/rp2040_flash/rp2040_flash ~/FLY_Katapult/BL/SB2040/FLY_SB2040PROV3_CANBOOT_CAN_500K.uf2
                ```
                       
            * Ci-dessous est la commande de flashage du firmware pour une vitesse CAN de 1M
                ```bash
                sudo ~/klipper/lib/rp2040_flash/rp2040_flash ~/FLY_Katapult/BL/SB2040/FLY_SB2040PROV3_CANBOOT_CAN_1M.uf2
                ```


            5. Si vous voyez ci-dessous, cela signifie que le flashage a réussi

                <ImageView image={require('@site/docs/General/dfu-flash/img/flash3.webp').default} size="100%" align="center" />

            :::tip

            Après le flashage, il est nécessaire de couper l'alimentation de la carte mère une fois.

            :::

        </TabItem>
    </Tabs>

## Vérifier si le firmware est démarré

    * Si le firmware démarre correctement, la LED clignotera
        <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-sb2040/sb2040-pro-v3/flash/img/led.webp').default} size="100%" align="center" />
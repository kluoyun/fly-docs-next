---
sidebar_position: 1
---

import ImageView from '@site/src/components/ImageView';

# Klipper常见错误

----

## mcu 'xxx': Unable to connect

 1. **报错信息**

   <ImageView image={require('./img/cannot_connect.png').default} size="80%" align="center" />

 2. **报错原因**

 * 上位机无法找到主板并且连接

 3. **解决方法**

 * 查看Klippy.log翻到最下方
 * 出现`[Errno 2]`代表没有搜索ID并且添加到**printer.cfg**里面

   <ImageView image={require('./img/errno2.png').default} size="80%" align="center" />

 * 出现**serialhdl.error: mcu 'xxx': Serial connection closed**代表CAN ID没有找到或者填写错误，需要搜索ID并且填写进去

   <ImageView image={require('./img/serial_connection_closed.png').default} size="80%" align="center" />
 
 * 出现**mcu 'mcu': Unable to open CAN port: [Errno 19] No such device**代表没有**UOTC或者刷了CAN桥接的主板**

   <ImageView image={require('./img/serial_connection_closed.png').default} size="80%" align="center" />

 * 出现**mcu 'mcu': Unable to open CAN port: Failed to transmit: [Errno 100] Network is down**代表**CAN网络没配置或者CAN0没启动**

   <ImageView image={require('./img/serial_connection_closed.png').default} size="80%" align="center" />

 * 配置方法: 在SSH中输入下面指令后重启系统
```bash
sudo /bin/sh -c "cat > /etc/network/interfaces.d/can0" << EOF
allow-hotplug can0
iface can0 can static
    bitrate 1000000
    up ifconfig $IFACE txqueuelen 1
    pre-up ip link set can0 type can bitrate 1000000
    pre-up ip link set can0 txqueuelen 1
EOF
```
* 出现**mcu 'mcu': Unable to open CAN port: Failed to transmit: [Errno 105] Network is down**代表**CAN网络的缓存太小**解决方法将缓存改大

## mcu 'mcu': Invalid CAN uuid

 1. **报错信息**

   <ImageView image={require('./img/lnvalid_can_uuid.png').default} size="80%" align="center" />

 2. **报错原因**

 * CAN ID没有填写或者填写错误

 3. **解决方法**

 * 需要搜索ID并且填写进去

## Option 'serial' in section 'mcu' must be specified

 1. **报错信息**

   <ImageView image={require('./img/serial.png').default} size="80%" align="center" />

 2. **报错原因**

 * USB ID没有填写或者填写错误

 3. **解决方法**

 * 需要搜索ID并且填写进去

##  homing override method always homes X and Y before homing Z. The safe_z_home

 1. **报错信息**

   <ImageView image={require('./img/homing.png').default} size="80%" align="center" />

 2. **报错原因**

 * 重复配置`[safe_z_home]`与`[homing_override]`

 3. **解决方法**

 * 只需要配置其中一项，另外一个删除即可
services:
  mellow:
    container_name: fly-docs
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BUILD_LOCALE: ${BUILD_LOCALE:-all}
    ports:
      - "9521:80"
    networks:
      - fly_net
networks:
  fly_net:
    driver: bridge

---
sidebar_position: 6
sidebar_label: BL firmware, please ignore

---

import WIN from '@site/i18n/en/docusaurus-plugin-content-docs/current/General/dfu-flash/_pc-uf2-flash.mdx';
import Katapult from '@site/i18n/en/docusaurus-plugin-content-docs/current/General/dfu-flash/_host-uf2-flash.mdx';
import CANBOOT from '@site/i18n/en/docusaurus-plugin-content-docs/current/General/dfu-flash/_firmware/katapult/_katapult.mdx';

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import Button from "@mui/material/Button";
import ImageView from '@site/src/components/ImageView';




# Katapult Firmware

## Katapult Firmware Description

    <CANBOOT />

    :::tip

    * SB2040_V3 supports CAN and RS232
    * Only CAN firmware at 500K and 1M baud rates are provided here

    :::

## Entering Programming Mode

    :::tip

    The left is the `RESET button`, and the right is the `BOOT button`.

    :::

    * There are two methods to enter programming mode.
    * Method one: With the main board completely powered off, hold the BOOT button and then connect the data transmission cable to both the main board and your device.
    * Method two: Connect the data transmission cable to both the main board and your device, hold the BOOT button, press the RST button, release the RST button, and finally release the BOOT button.
        <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-sb2040/sb2040-v3/flash/img/boot.png').default} size="100%" align="center" />

## Flashing Firmware
    <Tabs>
        <TabItem value="win" label="Windows Flash Tutorial" default>

            * The Katapult firmware for SB2040 V3 <Button variant="contained" disableElevation href="https://cdn.mellow.klipper.cn/firmware/BL/SB2040_V3.zip">Click to automatically download the firmware</Button>
            <WIN />
            
        </TabItem>
        <TabItem value="linux" label="Host Machine Flash Tutorial">

            <Katapult />

            4. Flash Firmware

            :::tip

            Only one firmware needs to be flashed.

            :::

            * Below is the command to flash the 500K CAN baud rate firmware:
                ```bash
                sudo ~/klipper/lib/rp2040_flash/rp2040_flash ~/FLY_Katapult/BL/SB2040/FLY_SB2040V3_CANBOOT_CAN_500K.uf2
                ```

            * Below is the command to flash the 1M CAN baud rate firmware:
                ```bash
                sudo ~/klipper/lib/rp2040_flash/rp2040_flash ~/FLY_Katapult/BL/SB2040/FLY_SB2040V3_CANBOOT_CAN_1M.uf2
                ```

            5. If you see the prompt below, the flashing is successful.

                <ImageView image={require('@site/docs/General/dfu-flash/img/flash3.png').default} size="100%" align="center" />

            :::tip

            After flashing, you need to power off the main board completely.

            :::

        </TabItem>
    </Tabs>

## Confirming Firmware Boot

    * If the firmware starts up normally, the LED will blink.
        <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-sb2040/sb2040-v3/flash/img/led.png').default} size="100%" align="center" />
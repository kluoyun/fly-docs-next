* 上位機が正常にインターネットに接続できることを確認し、下位機と上位機を接続するデータ線にデータ転送機能があることを確認してください。
* SSHで上位機に接続した後、コマンドラインに`lsusb`と入力してエンターを押します。以下に示す画像内の囲まれた情報が表示された場合、次のステップに進むことができます。
    * 何も情報を返さない場合は、これは上位機のシステムの問題であり、こちらでは対処できません。正常なシステムに変更するか、または上位機を交換する必要があります。
    * `lsusb`コマンドがないというメッセージが表示された場合は、次のコマンドを実行してインストールすることができます
        ```bash
        sudo apt-get install usbutils
        ```
        <ImageView image={require('@site/docs/General/get-id/img/6177.webp').default} size="80%" align="left" />
* `1d50:6177`は今回使用するデバイスに属しています。`OpenMoko, Inc. stm32f407xx`のサフィックスは一定の参考価値がありますが、判断の根拠とはなりません。
    * 一部の上位機はシステムの問題により、完全に表示されないか、表示されないことがあります。
    * `lsusb`コマンドを入力するとデバイスが表示されるが、`1d50:6177`がない場合は、データ線を交換してマザーボードを上位機の他のUSBポートに接続してみてください。
* 上記の手順を以前に実行し、Klipperファームウェアを正常に焼入れし、マザーボードが正常に動作している場合、Klipperファームウェアを更新するだけの場合、このページ右側のメニューにある`ファームウェア更新`セクションを確認してください。

:::warning 注意事項
   `1d50:6177`デバイスを照会した後でなければ、次のステップに進めません
:::

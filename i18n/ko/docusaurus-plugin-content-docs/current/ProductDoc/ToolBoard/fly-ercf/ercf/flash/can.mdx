---
sidebar_position: 3
sidebar_label: CAN 펌웨어 플래시
---

import CANFLASH from '@site/i18n/ko/docusaurus-plugin-content-docs/current/General/dfu-flash/_host-can-flash.mdx';
import MakeOperate from "/docs/General/klipper-make/_make-operate.mdx";
import CAN from '@site/i18n/ko/docusaurus-plugin-content-docs/current/General/dfu-flash/_firmware/2040/_can.mdx';
import SSH from '@site/i18n/ko/docusaurus-plugin-content-docs/current/General/dfu-flash/_firmware/_ssh.mdx';

import LS from '@site/i18n/ko/docusaurus-plugin-content-docs/current/General/dfu-flash/_can.mdx';

# CAN 펌웨어 컴파일

## 컴파일 시작

    <SSH />

    <MakeOperate/>

    <CAN />

    * GPIO 핀 선택 마이크로 컨트롤러 시작 시 설정, 입력: gpio17
        <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-ercf/ercf/flash/img/can.webp').default} size="100%" align="center" />
    * `Q` 키를 누르면 **Save configuration**이 나타납니다. 이때 `Y` 키를 다시 누릅니다.
        <ImageView image={require('@site/docs/General/dfu-flash/img/configuration.webp').default} size="50%" align="center" />
    * 이제 설정이 저장되었고 명령줄 인터페이스로 돌아갔을 것입니다.
    * `make -j4`를 입력하여 컴파일을 시작합니다. 시간이 좀 걸립니다.
        <ImageView image={require('@site/docs/General/dfu-flash/img/make.webp').default} size="50%" align="center" />
    * 마지막으로 아래와 같은 내용이 출력되면 컴파일이 성공적으로 완료된 것입니다.

        ```bash
        Linking out/klipper.elf
        Creating hex file out/klipper.bin
        ```

## 장치 검색

    <LS />


## 펌웨어 플래시

    * 펌웨어 플래시 전에 XT30 데이터 케이블을 사용하여 도구판을 UTOC 또는 브리지 펌웨어가 설치된 메인보드에 연결하십시오. 아래는 참조 연결도입니다.
        <ImageView image={require('@site/docs/General/get-id/img/utoc.webp').default} size="80%" align="left" />
    * CAN H와 CAN L의 저항값이 전원이 꺼진 상태에서 멀티미터로 측정했을 때 `60Ω` 정도인지 확인하십시오. 만약 `120Ω`이면 `120Ω` 점퍼를 연결해야 하고, `40Ω` 정도면 점퍼를 제거해야 합니다.
        <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-ercf/ercf/flash/img/120.webp').default} size="50%" align="center" />

    <CANFLASH />

## 펌웨어가 정상적으로 시작했는지 확인

    * 펌웨어가 정상적으로 시작되면 LED가 켜집니다.
        <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-ercf/ercf/flash/img/led.webp').default} size="100%" align="center" />


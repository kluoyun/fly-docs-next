---
sidebar_position: 3
sidebar_label: CAN 펌웨어 프래싱

---

import CANFLASH from '@site/i18n/ko/docusaurus-plugin-content-docs/current/General/dfu-flash/_host-can-flash.mdx';
import MakeOperate from "/docs/General/klipper-make/_make-operate.mdx";
import CAN from '@site/i18n/ko/docusaurus-plugin-content-docs/current/General/dfu-flash/_firmware/2040/_can1.mdx';
import SSH from '@site/i18n/ko/docusaurus-plugin-content-docs/current/General/dfu-flash/_firmware/_ssh.mdx';

import LS from '@site/i18n/ko/docusaurus-plugin-content-docs/current/General/dfu-flash/_can.mdx';

# CAN 펌웨어 빌드

## 빌드 시작

    <SSH />

    <MakeOperate/>

    <CAN />

    * **마이크로컨트롤러 시작 시 설정할 GPIO 핀 선택**을 선택하고 입력: **!gpio5,!gpio19,!gpio24**
    :::warning
        영문 입력 모드에서 `!gpio5,!gpio19,!gpio24`를 입력해야 합니다.
    :::
        <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-sb2040/sb2040-v3/flash/img/can.webp').default} size="100%" align="center" />
    * `Q` 키를 누르면 **Save configuration**이 나타나고, 그 후에 `Y` 키를 누릅니다
        <ImageView image={require('@site/docs/General/dfu-flash/img/configuration.webp').default} size="50%" align="center" />
    * 이제 구성이 저장되었으며 명령줄 화면으로 돌아왔습니다
    * 아래 명령어를 입력하여 빌드를 시작합니다 (시간이 다소 소요됩니다)
    ```bash
    make -j4
    ```
        <ImageView image={require('@site/docs/General/dfu-flash/img/make.webp').default} size="50%" align="center" />
    * 마지막에 다음 내용이 출력되면 빌드 성공입니다

        ```bash
        Linking out/klipper.elf
        Creating bin file out/klipper.bin
        ```

    <LS />


## 펌웨어 프래싱

    * 펌웨어를 프래싱하기 전에 XT30 데이터 케이블을 도구 보드를 UTOC 또는 브릿지 펌웨어가 설치된 메인보드에 연결합니다. 아래는 참조 연결도입니다
    * 전환 스위치가 올바른 위치에 있는지 확인하세요
        <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-sb2040/sb2040-v3/flash/img/sb2040v3_utoc.webp').default} size="80%" align="center" />
    * CAN H와 CAN L의 저항값을 전원이 꺼진 상태에서 멀티미터로 측정하며, 약 **60Ω** 근처인지 확인하세요. 만약 **120Ω**라면 **120Ω** 스위치를 켜야 하며, **40Ω** 근처라면 스위치를 끄세요
        <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-sb2040/sb2040-v3/flash/img/120.webp').default} size="100%" align="center" />

    <CANFLASH />

## 펌웨어 정상 작동 여부 확인

    * 펌웨어가 정상적으로 시작되면 LED가 켜집니다
        <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-sb2040/sb2040-v3/flash/img/led.webp').default} size="100%" align="center" />


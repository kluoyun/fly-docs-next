---
sidebar_position: 5
sidebar_label: RRF에 연결
---

# RRF에 연결

    :::info 중요 팁
    * AirClick이 대상 프린터와 동일한 네트워크에 연결되어야 합니다.
    :::

1. 왼쪽 시스템 설정을 클릭합니다.

<ImageView image={require('@site/docs/ProductDoc/ExtensionBoard/fly-airclick/img/setting.webp').default} size="80%" align="Center" />

2. `Printer Type`를 `RRF`로 선택합니다.
3. `RepRapFirmware IP`에 RRF 기기의 IP 주소를 입력합니다.

    <ImageView image={require('@site/docs/ProductDoc/ExtensionBoard/fly-airclick/img/rrf.webp').default} size="80%" align="Center" />

4. 위의 구성이 완료되면, 오른쪽 하단의 `제출`을 클릭하여 설정을 저장합니다.
5. 올바르게 연결된 후 웹 페이지 상단에 메시지가 표시됩니다.

     <ImageView image={require('@site/docs/ProductDoc/ExtensionBoard/fly-airclick/img/setting-3.webp').default} size="80%" align="Center" />

6. 연결 실패 시
    - IP 주소가 올바른지 확인합니다.
    - 프린터와 AirClick이 동일한 네트워크에 연결되어 있는지 확인합니다.
    - RRF 보드가 정상적으로 작동하는지 확인합니다.
    - 브라우저에서 RRF 웹 페이지를 열 수 있는지 확인합니다.

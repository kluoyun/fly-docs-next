---
sidebar_position: 1
sidebar_label: 固件烧录说明
---





    :::tip

    * Micro4默认使用USB连接方式的Katapult进行固件烧录

    :::

# 固件烧录说明

:::danger 须知
* 主板固件烧录需要通过Katapult烧录Klipper
* 如果确定主板已经烧录了Katapult则无需进行最后面的烧录Katapult固件，那只是预防主板的Katapult固件没烧录成功
* 可以快速按两次重置后看主板上是否有一个LED闪烁起来，如果有则代表主板已经进去Katapult固件
:::

## 如何判断你需要的哪一种固件

    :::tip

    * 请注意你只需要刷其中一种固件即可

    :::
    
    * 可以使用以下方式连接上位机
    * <Button variant="contained" disableElevation component="a" rel="noopener" href="./usb">USB固件</Button>
        * 好处：通信可靠，稳定
        * 坏处：如果多个USB设备可能会影响数据传输
    * <Button variant="contained" disableElevation component="a" rel="noopener" href="./can">USB桥接固件</Button>
        * 好处：可以接多个设备并且不占用USB口
        * 坏处：可能因使用`KlipperScreen`一起使用可能导致归位超时等报错。

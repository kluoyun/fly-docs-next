Nachfolgend wird erläutert, wie man die Firmware kompiliert:

* Verbinden Sie sich über SSH und geben Sie den untenstehenden Befehl ein und drücken Sie Enter:

    ```bash
    cd ~/klipper && rm -rf ~/klipper/.config && rm -rf ~/klipper/out && make menuconfig
    ```

* Wobei `rm -rf ~/klipper/.config && rm -rf ~/klipper/out` die vorherigen Kompilierungsdaten und Firmware löscht und `make menuconfig` die Firmware kompiliert. Nach der Ausführung sollte die folgende Benutzeroberfläche erscheinen:

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/img/make1.png').default} size="100%" align="Center" />

* Wählen Sie **Zusätzliche niedrigstufige Konfigurationsoptionen aktivieren** und drücken Sie Enter:

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/img/make2.png').default} size="100%" align="Center" />

* Wechseln Sie in das Menü **Mikrocontroller-Architektur** und wählen Sie **STMicroelectronics STM32** und drücken Sie Enter:

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/img/make3.png').default} size="100%" align="Center" />

* Gehen Sie in das Menü **Prozessormodell**, wählen Sie **STM32F072** und drücken Sie Enter:

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/072/img/072.png').default} size="100%" align="Center" />

* Wählen Sie **Bootloader-Offset**, wählen Sie: **8KiB Bootloader**

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/072/img/8kb.png').default} size="100%" align="Center" />

* **Kommunikationsschnittstelle**, wählen Sie: **USB (auf PA11/PA12)**

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/072/img/usb.png').default} size="100%" align="Center" />
import ImageView from '@site/src/components/ImageView';

    :::danger 须知

    * FLY上位机需要使用M2WE或者2.4G WIFI 模块否则使用WiFi
    * 请注意不支持中文名字的WIFI与2.4G和5G双频合一的WIFI
    * 如果是mini pad或者lite2请无视此警告

    :::

## 连接上位机

    * 如果在BOOT盘下配置WIFI失败可以参考此配置进行WIFI连接，请注意需要使用串口连接上位机
    * 请先使用串口[连接到SSH](/docs/DebugDoc/BasicTutorial/index.mdx "点击即可跳转") 
    * 执行下面的命令, 可能会提示输入密码，输入密码`mellow`回车，请注意密码不可见

    ```cfg
    sudo nmtui
    ```

        <ImageView image={require('./img/nmtui/nmtui1.png').default} size="80%" align="left" />

        <ImageView image={require('./img/nmtui/nmtui2.png').default} size="80%" align="left" />

    * 选择`Activate a connection`后回车

        <ImageView image={require('./img/nmtui/nmtui3.png').default} size="80%" align="left" />

    * 进入如下界面，通过键盘上的`↑ ↓`键选择要连接的WiFi后，回车，输入密码后等待连接成功

        <ImageView image={require('./img/nmtui/nmtui4.png').default} size="80%" align="left" />


        <ImageView image={require('./img/nmtui/nmtui5.png').default} size="80%" align="left" />

    * 连接成功后如图所示

        <ImageView image={require('./img/nmtui/nmtui6.png').default} size="80%" align="left" />

    * 按ESC键退出，返回命令行界面，输入`ip a`，图中`192.168.2.126`即为上位机的IP地址

        <ImageView image={require('./img/nmtui/nmtui7.png').default} size="80%" align="left" />

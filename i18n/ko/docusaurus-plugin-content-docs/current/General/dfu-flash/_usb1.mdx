* 상위 컴퓨터가 정상적으로 연결될 수 있도록 하며, 하위 컴퓨터와 연결된 데이터선이 데이터 전송 기능을 가지고 있는지 확인하세요.
* 상위 컴퓨터에 SSH로 연결한 후 `lsusb`를 입력하고 Enter 키를 누릅니다. 아래 이미지에서 동그라미 표시된 정보가 나타나면 다음 단계로 진행할 수 있습니다.
    * 아무런 정보도 반응이 없으면, 이는 상위 컴퓨터의 시스템 문제입니다. 여기서는 해결할 수 없으므로, 정상적인 시스템으로 교체하거나 상위 컴퓨터를 교체해야 합니다.
    * `lsusb` 명령이 없다고 표시되면 다음 명령어를 실행하여 설치할 수 있습니다.
        ```bash
        sudo apt-get install usbutils
        ```
        <ImageView image={require('@site/docs/General/get-id/img/614e.webp').default} size="80%" align="left" />
* `1d50:614e`는 이번에 사용할 장치입니다. `OpenMoko, Inc. stm32f407xx`라는 후缀은 일정 부분 참고 가치가 있지만, 판단 기준은 아닙니다.
    * 일부 상위 컴퓨터는 시스템 문제로 인해 장치 정보가 완전히 표시되지 않거나 표시되지 않을 수 있습니다.
    * 만약 `lsusb` 명령어를 입력하면 장치가 표시되지만 `1d50:614e`가 보이지 않는 경우, 데이터선을 교체하거나 메인보드를 상위 컴퓨터의 다른 USB 포트에 연결해 보세요.
* 이전에 위 단계를 수행했으며 Klipper 펌웨어를 성공적으로 번역하고 메인보드가 정상적으로 작동하는 경우, Klipper 펌웨어를 업데이트하려면 본 페이지 오른쪽 메뉴바의 `펌웨어 업데이트` 섹션을 참조하세요.

:::warning 주의사항
하나 이상의 `1d50:614e` 장치를 찾은 후에만 다음 단계로 진행할 수 있습니다.
:::

import ImageView from '@site/src/components/ImageView';


* 短接BOOT，使用type c连接到电脑会出现`RPI-RP2`
* 弹出`RPI-RP2`后不需要在短接BOOT或者一直按按钮

<ImageView image={require('@site/docs/General/dfu-flash/_firmware/2040/img/rp2.png').default} size="100%" align="left" />

* 把固件复制进去后`RPI-RP2`会消失，然后固件灯会闪烁
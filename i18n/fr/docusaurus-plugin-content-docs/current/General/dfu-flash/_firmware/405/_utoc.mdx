Voici comment compiler le firmware :
* Connectez-vous à SSH et entrez les commandes suivantes, puis appuyez sur Entrée :

    ```bash
    cd ~/klipper && rm -rf ~/klipper/.config && rm -rf ~/klipper/out && make menuconfig
    ```

* Dans `rm -rf ~/klipper/.config && rm -rf ~/klipper/out`, il s'agit de supprimer les données de compilation précédentes et le firmware. `make menuconfig` permet de compiler le firmware. Après exécution, l'interface suivante devrait apparaître :

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/img/make1.webp').default} size="100%" align="Center" />

* Sélectionnez **Activer les options de configuration de bas niveau** et appuyez sur Entrée

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/img/make2.webp').default} size="100%" align="Center" />

* Entrez dans le menu **Architecture du microcontrôleur** puis sélectionnez **STMicroelectronics STM32** et appuyez sur Entrée

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/img/make3.webp').default} size="100%" align="Center" />

Les images sont en cours de mise à jour
* Entrez dans le menu **Modèle de processeur**, sélectionnez **STM32F405** et appuyez sur Entrée

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/405/img/405.webp').default} size="100%" align="Center" />

* Sélectionnez **Décalage du chargeur d'amorçage**, choisissez : **Chargeur d'amorçage de 32KiB**

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/405/img/32kb.webp').default} size="100%" align="Center" />

* Sélectionnez **Interface de communication**, choisissez : **Pont USB vers bus CAN (USB sur PA11/PA12)**
* Sélectionnez **Interface bus CAN** selon l'image **CAN bus （on PB8/PB9） --->**

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/405/img/utoc.webp').default} size="100%" align="Center" />
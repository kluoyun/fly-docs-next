---
sidebar_position: 1
sidebar_label: 固件烧录说明
---


import CANBOOT from '@site/docs/General/dfu-flash/_firmware/katapult/_katapult.mdx';



    :::tip

    * SB2040 PRO出厂预刷CAN连接的Katapult固件，速率为1M
    * SB2040 PRO CAN连接的Katapult固件速率分500K与1M，有需要可以自行烧录
    * [Katapult固件烧录](./bl.mdx "点击即可跳转")


    :::

# 固件烧录说明

    * SB2040 PRO可以使用CAN连接方式与上位机连接
    * CAN需要使用使用`XT30(2+2)-F`进行连接
    * 下方是`XT30(2+2)-F`数据线样式
    <ImageView image={require('@site/docs/General/get-id/img/xt30.webp').default} size="80%" align="left" />

:::danger 须知
* 主板固件烧录需要通过Katapult烧录Klipper
* 如果确定主板已经烧录了Katapult则无需进行最后面的烧录Katapult固件，那只是预防主板的Katapult固件没烧录成功
* 可以快速按两次重置后看主板上是否有一个LED闪烁起来，如果有则代表主板已经进去Katapult固件
:::

## 如何判断你需要的哪一种固件

    * SB2040 PRO可以使用以下方式连接上位机
    * [CAN固件](./can.mdx "点击即可跳转")
        * 好处：如果刷了`Katapult`固件可以无需触碰主板情况下更新固件，并且支持总线连接
        * 坏处：可能因使用`KlipperScreen`一起使用可能导致归位超时等报错。
        * 提示：如果需要使用CAN请确定上位机是否支持CAN，并且需要搭配UTOC或者刷了桥接固件的主板


## Katapult固件说明

    * SB2040 PRO默认自带CAN连接速率1M的Katapult固件
    
    <CANBOOT />

---
sidebar_position: 6
sidebar_label: 添加USB摄像头
---

import ImageView from '@site/src/components/ImageView';

# 添加USB摄像头


:::danger 须知

* 需要有安装`crowsnest`服务，如lite2与mini pad的系统未安装
* 需要免驱即插即用摄像头，均可以在linux、armbian等系统下的klipper支持使用，无需另外安装驱动。
* 本教程使用最新klipper，crowsnest，fuidd为例，如果是其他方法需要自行研究

:::

## 打开配置

:::tip

* 摄像头id根据系统有很多种，我们只需要确定不会变的ID即可

:::

    * 在Fuidd中找到配置文件，然后打开`crowsnest.conf`
        <ImageView image={require('./img/cam/conf.png').default} size="100%" align="center" />

    * 找打`device: `将后面的ID配置删除
        <ImageView image={require('./img/cam/conf1.png').default} size="100%" align="center" />

## 搜索摄像头ID

:::tip

* 请确保摄像头已经接到上位机

:::

    * 点击上方`DEVICES`，然后点击`VIDEO`,最后点击刷新
        <ImageView image={require('./img/cam/conf2.png').default} size="100%" align="center" />
    * 寻找到`path_by_id`将后面一行复制
        <ImageView image={require('./img/cam/conf3.png').default} size="100%" align="center" />

## 添加ID

    * 添加ID，并且保存配置
        <ImageView image={require('./img/cam/conf4.png').default} size="100%" align="center" />

## 启用摄像头

    * 点击设置，选择摄像头，然后点击添加摄像头
        <ImageView image={require('./img/cam/conf5.png').default} size="100%" align="center" />
    * 输入自己喜欢的名字,并且点击保存
        <ImageView image={require('./img/cam/conf6.png').default} size="100%" align="center" />

## 查看是否启用

    * 点击仪表盘正常已经出现摄像头画面
        <ImageView image={require('./img/cam/conf7.png').default} size="100%" align="center" />
    * 如果不显示重启系统即可
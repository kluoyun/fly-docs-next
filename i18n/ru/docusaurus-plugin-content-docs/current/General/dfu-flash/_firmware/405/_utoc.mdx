Вот как скомпилировать прошивку:
* Подключитесь через SSH и введите следующие команды, затем нажмите Enter:

    ```bash
    cd ~/klipper && rm -rf ~/klipper/.config && rm -rf ~/klipper/out && make menuconfig
    ```

* Здесь `rm -rf ~/klipper/.config && rm -rf ~/klipper/out` удаляет предыдущие данные компиляции и прошивку, `make menuconfig` компилирует прошивку, после выполнения должно появиться следующее окно:

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/img/make1.png').default} size="100%" align="Center" />

* Выберите **Включить дополнительные опции конфигурации на низком уровне** и нажмите Enter:

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/img/make2.png').default} size="100%" align="Center" />

* Войдите в меню **Архитектура микроконтроллера** и выберите **STMicroelectronics STM32**, затем нажмите Enter:

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/img/make3.png').default} size="100%" align="Center" />

Изображения обновляются
* Войдите в меню **Модель процессора** и выберите **STM32F405**, затем нажмите Enter:

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/405/img/405.png').default} size="100%" align="Center" />

* Выберите **Смещение загрузчика**, выберите: **32KiB загрузчик**

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/405/img/32kb.png').default} size="100%" align="Center" />

* Выберите **Интерфейс связи**, выберите: **USB to CAN bus bridge (USB на PA11/PA12)**

    <ImageView image={require('@site/docs/General/dfu-flash/_firmware/405/img/utoc.png').default} size="100%" align="Center" />
Voici comment compiler le firmware :

*   Connectez-vous à SSH et entrez la commande suivante puis appuyez sur Entrée :

        ```bash
        cd ~/klipper && rm -rf ~/klipper/.config && rm -rf ~/klipper/out && make menuconfig
        ```

*   Parmi `rm -rf ~/klipper/.config && rm -rf ~/klipper/out`, il s'agit de supprimer les données de compilation et le firmware précédents `make menuconfig` compile le firmware. Après exécution, vous devriez voir l'interface ci-dessous :

        <ImageView image={require('@site/docs/General/dfu-flash/_firmware/img/make1.webp').default} size="100%" align="Center" />

*   Sélectionnez **Enable extra low-level configuration options** et appuyez sur Entrée

        <ImageView image={require('@site/docs/General/dfu-flash/_firmware/img/make2.webp').default} size="100%" align="Center" />

*   Entrez dans le menu **Micro-controller Architecture** puis sélectionnez **STMicroelectronics STM32** et appuyez sur Entrée

        <ImageView image={require('@site/docs/General/dfu-flash/_firmware/img/make3.webp').default} size="100%" align="Center" />

Les images sont en cours de mise à jour

*   Entrez dans le menu **Processor model**, sélectionnez **STM32F405** et appuyez sur Entrée

        <ImageView image={require('@site/docs/General/dfu-flash/_firmware/405/img/405.webp').default} size="100%" align="Center" />

*   Sélectionnez **Bootloader offset**, choisissez : **32KiB bootloader**

        <ImageView image={require('@site/docs/General/dfu-flash/_firmware/405/img/32kb.webp').default} size="100%" align="Center" />

*   Sélectionnez **Communication interface**, choisissez : **USB to CAN bus bridge (USB on PA11/PA12)**
*   Selon l'image, sélectionnez **CAN bus  interface （CAN bus （on PB8/PB9）） --->**

        <ImageView image={require('@site/docs/General/dfu-flash/_firmware/405/img/utoc.webp').default} size="100%" align="Center" />
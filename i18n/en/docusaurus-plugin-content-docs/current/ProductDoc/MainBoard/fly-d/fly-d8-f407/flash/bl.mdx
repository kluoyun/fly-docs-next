---
sidebar_position: 6
sidebar_label: BL firmware, please ignore
---

import WIN from '@site/i18n/en/docusaurus-plugin-content-docs/current/General/dfu-flash/_pc-dfu-flash.mdx';
import Katapult from '@site/i18n/en/docusaurus-plugin-content-docs/current/General/dfu-flash/_host-dfu-katapult-flash.mdx';
import CANBOOT from '@site/i18n/en/docusaurus-plugin-content-docs/current/General/dfu-flash/_firmware/katapult/_katapult.mdx';

# Katapult firmware

## Katapult firmware description

    <CANBOOT />

    :::tip

    * D8 uses USB connection

    :::

## Enter programming mode

    :::tip

    Left is the `RST button`, right is the `BOOT button`

    :::

    * There are two ways to enter programming mode
    * First method: With the motherboard completely powered off, press and hold the BOOT button then connect the data transfer cable one end to the motherboard and the other end to your device.
    * Second method: After connecting the data transfer cable one end to the motherboard and the other end to your device, press and hold the BOOT button then press the RST button, then release the RST button and finally release the BOOT button
        <ImageView image={require('@site/docs/ProductDoc/MainBoard/fly-d/fly-d8-f407/flash/img/boot.webp').default} size="50%" align="center" />

## Flash firmware
    <Tabs>
        <TabItem value="win" label="Windows flashing tutorial" default>

            * Katapult firmware for D8 <Button variant="contained" disableElevation component="a" rel="noopener" href="https://cdn.mellow.klipper.cn/firmware/BL/D.zip">Click to automatically download firmware</Button>
            <WIN />

        </TabItem>
        <TabItem value="linux" label="Host machine flashing tutorial">

            <Katapult />

            4. Flash firmware

            :::tip

            Only one firmware needs to be flashed

            :::

            * Below is the firmware flashing command
                ```bash
                sudo dfu-util -a 0 -d 0483:df11 --dfuse-address 0x08000000 -D ~/FLY_Katapult/BL/D/FLY_D8_F407.bin
                ```

            5. If the content shown at the arrow in the following image is ``File downloaded successfully``, then the flashing is successful.

                <ImageView image={require('@site/docs/General/dfu-flash/img/make-flash.webp').default} size="100%" align="center" />

            :::tip

            After flashing, the motherboard needs to be completely powered off once.

            :::

        </TabItem>
    </Tabs>

## 設備の検索
    * 上位機が正常にネットワークに接続できていることを確認し、下位機と接続されているデータ線がデータ転送機能を持っていることを確認してください。
    * 上位機にSSH接続し、コマンド `lsusb` を入力してEnterを押します。もし画像で囲まれた情報が表示された場合、次のステップに進むことができます。
        * 何も情報が返ってこない場合、これは上位機のシステムの問題です。こちらでは対応できませんので、正常なシステムに切り替えるか、上位機を交換してください。
        * `lsusb` コマンドがないというメッセージが表示された場合、以下のコマンドを実行してインストールできます。
            ```bash
            sudo apt-get install usbutils
            ```
            <ImageView image={require('@site/docs/General/get-id/img/606f.webp').default} size="70%" align="center" /> 
    * 赤枠内に表示されるIDは参考にはなりますが、`固定IDではなく変更可能`であることに注意してください。`OpenMoko, Inc. Geschwister Schneider CAN adapter` というサフィックスは判断の基準となります。
    * システムの問題により、一部の上位機では完全に表示されなかったり、全く表示されなかったりすることがあります。

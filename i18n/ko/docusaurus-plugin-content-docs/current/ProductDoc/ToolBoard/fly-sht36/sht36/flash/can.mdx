---
sidebar_position: 3
sidebar_label: CAN 펌웨어 플래시
---

import CANFLASH from '@site/i18n/ko/docusaurus-plugin-content-docs/current/General/dfu-flash/_host-can-flash.mdx';
import MakeOperate from "/docs/General/klipper-make/_make-operate.mdx";
import CAN from '@site/i18n/ko/docusaurus-plugin-content-docs/current/General/dfu-flash/_firmware/072/_can.mdx';
import SSH from '@site/i18n/ko/docusaurus-plugin-content-docs/current/General/dfu-flash/_firmware/_ssh.mdx';

import LS from '@site/i18n/ko/docusaurus-plugin-content-docs/current/General/dfu-flash/_can.mdx';

# CAN 펌웨어 컴파일

## 빌드 시작

    <SSH />

    <MakeOperate/>

    <CAN />

    * `Q` 키를 누르면 **Save configuration**이 나타나고, 그 다음에 `Y` 키를 누릅니다.
        <ImageView image={require('@site/docs/General/dfu-flash/img/configuration.webp').default} size="50%" align="center" />
    * 이제 설정이 저장되고 명령줄 인터페이스로 돌아갑니다.
    * `make -j4`를 입력하여 빌드를 시작합니다. 시간이 좀 걸립니다.
        <ImageView image={require('@site/docs/General/dfu-flash/img/make.webp').default} size="50%" align="center" />
    * 마지막으로 아래의 내용이 출력되면 빌드가 성공한 것입니다.

        ```bash
        Linking out/klipper.elf
        Creating hex file out/klipper.bin
        ```

## 장치 검색

    <LS />


## 펌웨어 플래시

    * 펌웨어 플래시 전에 XT30 데이터 케이블을 도구 보드를 UTOC 또는 다리 펌웨어가 설치된 메인보드에 연결합니다. 아래는 참조 접선도입니다.
        <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-sht36/sht36/img/utoc.webp').default} size="80%" align="left" />
    * CAN H와 CAN L가 전원이 꺼진 상태에서 멀티미터로 저항 값을 측정했을 때 약 `60Ω`이 나오면 정상입니다. 만약 `120Ω`라면 `120Ω` 점퍼를 연결해야 합니다. 만약 `40Ω` 정도라면 점퍼를 제거해야 합니다.
        <ImageView image={require('@site/docs/ProductDoc/ToolBoard/fly-sht36/sht36/flash/img/120.webp').default} size="100%" align="center" />

    <CANFLASH />
1. 설치 프로그램 설치

* FLY 상위 기기는 설치 프로그램을 실행할 필요가 없습니다.
* 설치가 필요한 경우 주의사항: 상위 기기는 인터넷 연결이 필요합니다.
* 이미 설치한 경우 다시 설치할 필요가 없습니다.

```bash
sudo apt update
sudo apt install dfu-util -y
```

2. 아래 명령어를 실행하여 연결이 성공했는지 확인하십시오.

```bash
lsusb
```

* DFU 장치가 없으면 **번인 모드**로 다시 진입해 보십시오.
* 시스템 버전 문제로 다른 이름이 표시될 수 있지만, `0483:df11`이 있으면 번인 모드로 진입한 것입니다.

<ImageView image={require('@site/docs/General/dfu-flash/img/lsusb.webp').default} size="100%" align="left" />


3. 미리 컴파일된 펌웨어 가져오기

* 여기에는 미리 컴파일된 펌웨어가 모두 포함되어 있습니다.
* 상위 기기는 인터넷 연결이 필요합니다.
* 상위 기기에서 다음 명령어를 실행하여 펌웨어 패키지를 다운로드하십시오.

```bash
cd && git clone https://github.com/FengHua741/FLY_Katapult.git
```

* 이미 펌웨어 패키지를 다운로드한 경우, 업데이트 후 나머지 작업을 진행하는 것이 좋습니다.

```bash
cd ~/FLY_Katapult && git pull
```
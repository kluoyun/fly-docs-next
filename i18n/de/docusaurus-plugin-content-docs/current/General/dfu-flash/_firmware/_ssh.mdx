import Button from "@mui/material/Button";


    * Sie benötigen einen Host mit installiertem `klipper`-Service  <Button variant="contained" disableElevation href="/docs/DebugDoc/BasicTutorial/">Verbindung über SSH herstellen</Button>
    * Und verbinden Sie das Gerät mit dem Host
    * Hinweis: Verwenden Sie bitte normale Hosts wie Raspberry Pi, FLY-Board usw. Bei modifizierten Hosts können wir aufgrund der vielen Probleme nur begrenzt Unterstützung bieten

    :::warning Beachten Sie

    * Verwenden Sie nicht den `ROOT`-Benutzer

    :::


1. 安装烧录工具

    * SSH中输入下方指令
    * 只有第一次烧录需要执行，后面无需在执行
    ```bash
    cd ~/klipper/lib/rp2040_flash/ && make
    ```
    <ImageView image={require('@site/docs/General/dfu-flash/img/2040.png').default} size="100%" align="left" />

2. 执行下面的命令查看是否连接成功。


    * 输入`lsusb`
    * 如果没有`2e8a:0003`设备请尝试重新进入**烧录模式**
    * 因系统版本问题可能显示其他名字，如果有`2e8a:0003`则代表进入烧录模式

        <ImageView image={require('@site/docs/General/get-id/img/2e8a.png').default} size="80%" align="left" /> 

    :::warning 注意事项

    有`2e8a:0003`时候才进行下一步

    :::

3. 拉取预先编译固件

    * 这里面都是预先编译好的固件
    * 请注意上位机需要能上网才可以安装
    * 上位机需要执行下方命令下载固件包

    ```bash
    cd && git clone https://github.com/FengHua741/FLY_Katapult.git
    ```

    * 如果已经下载过固件包的建议更新固件包后在进行剩下操作

    ```bash
    cd ~/FLY_Katapult && git pull
    ```


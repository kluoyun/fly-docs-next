---
sidebar_position: 2
sidebar_label: USB固件烧录
---

import Katapult from '@site/docs/General/dfu-flash/_host-katapult-flash.mdx';
import MakeOperate from "/docs/General/klipper-make/_make-operate.mdx";
import USB from '@site/docs/General/dfu-flash/_firmware/723/_usb1.mdx';
import SSH from '@site/docs/General/dfu-flash/_firmware/_ssh.mdx';
import ImageView from '@site/src/components/ImageView';
import LS from '@site/docs/General/dfu-flash/_usb.mdx';
import Button from "@mui/material/Button";

# USB 固件编译

    :::tip

    * MMU出厂预刷的CAN连接的Katapult固件
    * 如果使用USB固件烧录需要烧录USB的Katapult固件
    * 烧录一次后无需在烧录
    * <Button variant="contained" disableElevation href="./bl">Katapult固件烧录</Button>

    :::

## 开始编译

    <SSH />

    <MakeOperate/>

    <USB />

    * 选择GPIO pins to set at micro-controller startup，输入：!PA15 请注意!是大写
        <ImageView image={require('./img/usb.png').default} size="100%" align="center" />
    * 按 `Q` 键，出现 **Save configuration**，这时再按 `Y` 键
        <ImageView image={require('@site/docs/General/dfu-flash/img/configuration.png').default} size="50%" align="center" />
    * 现在应该保存了配置并且退出到了命令行界面
    * 输入 `make -j4` 开始编译，时间有点长
        <ImageView image={require('@site/docs/General/dfu-flash/img/make.png').default} size="50%" align="center" />
    * 最后输出下面的内容则编译成功

        ```bash
        Linking out/klipper.elf
        Creating hex file out/klipper.bin
        ```

## 确认是否进入烧录模式


    <LS />


## 烧录固件
    
    <Katapult />

## 确认固件是否启动

    * 如果固件正常启动LED会亮起来
        <ImageView image={require('./img/led.png').default} size="100%" align="center" />


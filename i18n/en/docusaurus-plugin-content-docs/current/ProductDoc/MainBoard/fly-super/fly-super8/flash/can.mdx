---
sidebar_position: 3
sidebar_label: USB Bridge CAN Firmware Flashing
---

import CANFLASH from '@site/i18n/en/docusaurus-plugin-content-docs/current/General/dfu-flash/_host-can-flash.mdx';
import MakeOperate from "/docs/General/klipper-make/_make-operate.mdx";
import CAN from '@site/i18n/en/docusaurus-plugin-content-docs/current/General/dfu-flash/_firmware/407/_utoc.mdx';
import SSH from '@site/i18n/en/docusaurus-plugin-content-docs/current/General/dfu-flash/_firmware/_ssh.mdx';

import LS from '@site/i18n/en/docusaurus-plugin-content-docs/current/General/dfu-flash/_can.mdx';
import TF from '@site/i18n/en/docusaurus-plugin-content-docs/current/General/dfu-flash/_pc-tf-flash.mdx';

# USB Bridge CAN Firmware Flashing

## Start Compiling

    <SSH />

    <MakeOperate/>

    <CAN />

    * Press the `Q` key, and when **Save configuration** appears, press the `Y` key
        <ImageView image={require('@site/docs/General/dfu-flash/img/configuration.webp').default} size="50%" align="center" />
    * The configuration should now be saved, and you should return to the command line interface
    * Enter `make -j4` to start compiling; it might take some time
        <ImageView image={require('@site/docs/General/dfu-flash/img/make.webp').default} size="50%" align="center" />
    * If the following content is output at the end, the compilation is successful

        ```bash
        Linking out/klipper.elf
        Creating hex file out/klipper.bin
        ```

## Flashing the Firmware


    <TF />

## Flashing the Firmware 2
    <Tabs>
        <TabItem value="linux" label="dfu flashing">
            * Follow the image below to insert the jumper cap `boot1` and then press `rst` to restart the motherboard
            <ImageView image={require('@site/docs/ProductDoc/MainBoard/fly-super/fly-super8-pro/flash/img/boot.webp').default} size="50%" align="center" />
            * Enter the following command to check if the motherboard has entered dfu mode
            ```bash
            lsusb
            ```
            * After pressing `Enter`, it will output the content shown in the image below. The content in the image indicates that the motherboard has successfully entered dfu mode
            <ImageView image={require('@site/docs/ProductDoc/MainBoard/fly-gemini/fly-gemini-v3/flash/img/dfu.webp').default} size="50%" align="center" />
           
            <dfu />

            4. Flashing the Firmware

            * Below is the dfu flashing command
                ```bash
                cd ~/klipper
                ```
                ```bash
                make flash FLASH_DEVICE=0483:df11
                ```
            5. If the content shown by the arrow in the image below appears, ``File downloaded successfully``, the flashing was successful

            :::warning

            Please ignore the **error messages following** the line indicated by the red arrow!!!

            :::

                <ImageView image={require('@site/docs/ProductDoc/MainBoard/fly-super/fly-super8/flash/img/boot.webp').default} size="100%" align="center" />

            :::tip

            After flashing is complete, remove the motherboard jumper cap and press `rst` to restart the motherboard

            :::

        </TabItem>
    </Tabs>
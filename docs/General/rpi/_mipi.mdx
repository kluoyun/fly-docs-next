import ImageView from '@site/src/components/ImageView';


## 树莓派5搭配MIPI

:::tip
FLY_MIPI是专门给树莓派5设计的转接模块，可通过转接直接使用DSI与CSI设备
:::

### 树莓派5搭配MIPI接线

    :::danger 须知

    * 树莓派5的DSI与CSI接口现在已经改名叫MIPI
    * MIPI接口可通过配置直接使用CSI摄像头或者DSI屏幕
    * 请注意金手指方向

    :::

    * 树莓派5现在有两个MIPI接口，如果正面看的话可以看到上面有标记1和0
    * 左边箭头在系统默认为DSI1,右边箭头在系统默认CAM0
        <ImageView image={require('@site/docs/General/rpi/img/mipi.png').default} size="100%" align="left" />

## DSI屏幕接线

    * 如果使用左边MIPI接口需要按下方配置屏幕

    ```bash
    dtoverlay=vc4-kms-dsi-7inch,dsi1
    ```

    * 如果使用右边MIPI接口需要按下方配置屏幕

    ```bash
    dtoverlay=vc4-kms-dsi-7inch,dsi0
    ```
    * 参考接线
        <ImageView image={require('@site/docs/General/rpi/img/mipi_dsi.png').default} size="100%" align="left" />

## CSI摄像头接线

    <ImageView image={require('@site/docs/General/rpi/img/csi1.png').default} size="100%" align="left" />
    * 请注意摄像头型号需要根据自己购买的进行配置，本教程使用imx219进行参考

    * 如果使用左边MIPI接口需要按下方配置摄像头
    ```bash
    dtoverlay=imx219,cam1
    ```

    * 如果使用右边MIPI接口需要按下方配置摄像头
    ```bash
    dtoverlay=imx219,cam0
    ```
    * 参考接线
    <ImageView image={require('@site/docs/General/rpi/img/mipi_csi.png').default} size="100%" align="left" />

